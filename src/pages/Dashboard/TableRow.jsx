import React from 'react';

const TableRow = ({ item, handleChange, index, getValue }) => {
    const { email, service_name, _id } = item

    return (
        <div>
            
        </div >
    );
};

export default TableRow;